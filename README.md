# مدیریت فروشگاه ووکامرس (Ideal WooManager)

اپلیکیشن اندروید (Kotlin + Jetpack Compose) برای **مدیریت سفارشات** و **حسابداری** فروشگاه‌های وردپرسی مبتنی بر **WooCommerce**. رابط کاربری کاملاً فارسی و راست‌به‌چپ (RTL) است و از طریق WooCommerce REST API v3 به فروشگاه شما متصل می‌شود.

---

## ✨ امکانات

### داشبورد
- کارت‌های شاخص: درآمد کل، فروش خالص، هزینه‌ها، **سود خالص**، تعداد سفارش، مالیات، حمل‌ونقل
- فیلتر بازه‌ی زمانی: امروز / ۷ روز اخیر / این ماه / امسال
- پرفروش‌ترین محصولات و آخرین سفارشات

### سفارشات
- فهرست سفارشات با صفحه‌بندی بی‌نهایت (infinite scroll)
- فیلتر بر اساس وضعیت (در انتظار، در حال پردازش، تکمیل‌شده، لغو، مسترد و…)
- جستجو بر اساس نام مشتری/شماره سفارش
- صفحه‌ی جزئیات کامل: اطلاعات مشتری، اقلام، جمع مالی، یادداشت
- **تغییر وضعیت سفارش** به‌صورت مستقیم از اپ

### حسابداری کامل
- **صورت سود و زیان**: فروش ناخالص، مالیات، حمل‌ونقل، تخفیف، فروش خالص، هزینه‌ها، سود خالص
- ثبت و مدیریت **هزینه‌ها** (به‌صورت محلی روی دستگاه با Room)
- دسته‌بندی هزینه‌ها (خرید کالا، حمل‌ونقل، تبلیغات، حقوق، اجاره، مالیات و…)
- تجمیع هزینه‌ها بر اساس دسته
- محاسبه‌ی خودکار سود خالص = فروش خالص − هزینه‌ها

### محصولات
- فهرست محصولات با تصویر (Coil)، قیمت، وضعیت موجودی و تعداد فروش
- جستجوی محصول

### تنظیمات
- اتصال به فروشگاه با آدرس سایت + Consumer Key/Secret
- تست اتصال
- تنظیم واحد پول (پیش‌فرض: تومان)

---

## 🧱 معماری و فناوری

| لایه | فناوری |
|------|--------|
| UI | Jetpack Compose + Material 3 |
| معماری | MVVM (ViewModel + StateFlow) |
| شبکه | Retrofit + OkHttp + kotlinx.serialization |
| پایگاه‌داده محلی | Room (هزینه‌ها) |
| تنظیمات | DataStore Preferences |
| تصاویر | Coil |
| ناوبری | Navigation Compose |

ساختار پکیج‌ها:
```
com.ideal.woomanager
├── data
│   ├── model        # DTOهای WooCommerce (Order, Product, Report)
│   ├── remote       # WooApi, ApiClient, AuthInterceptor
│   ├── local        # Room (Expense), SettingsStore (DataStore)
│   └── repository   # WooRepository
├── ui
│   ├── theme
│   ├── components   # کارت‌ها، حالت‌های Loading/Error/Empty
│   ├── navigation
│   └── screens      # dashboard / orders / accounting / products / settings
└── util             # Format, DateRange, OrderStatus, ...
```

---

## 🚀 راه‌اندازی

### پیش‌نیازها
- **Android Studio** (نسخه Ladybug یا جدیدتر توصیه می‌شود)
- **JDK 17**
- Android SDK با API 34

### باز کردن پروژه
1. این ریپازیتوری را در Android Studio باز کنید (`File → Open`).
2. Android Studio به‌صورت خودکار Gradle Sync را انجام می‌دهد و وابستگی‌ها را دانلود می‌کند.
3. یک دستگاه/شبیه‌ساز اندروید (API 24+) انتخاب کنید و روی **Run ▶** بزنید.

> نکته: فایل `local.properties` باید مسیر Android SDK را داشته باشد. Android Studio معمولاً این فایل را خودکار می‌سازد. در غیر این صورت از روی `local.properties.sample` بسازید.

### ساخت APK از خط فرمان
```bash
./gradlew assembleDebug      # خروجی: app/build/outputs/apk/debug/app-debug.apk
./gradlew assembleRelease    # نسخه‌ی انتشار (نیازمند امضا)
```

---

## 🔑 دریافت کلید API ووکامرس

1. وارد پیشخوان وردپرس شوید.
2. به مسیر **WooCommerce → Settings → Advanced → REST API** بروید.
3. روی **Add key** بزنید، توضیح دلخواه بنویسید و **Permissions** را روی **Read/Write** بگذارید.
4. کلید را ایجاد کنید و **Consumer Key** (`ck_...`) و **Consumer Secret** (`cs_...`) را کپی کنید.
5. در اپ، بخش **تنظیمات** → آدرس سایت + این دو کلید را وارد و **تست اتصال** بگیرید.

> ⚠️ فروشگاه باید روی **HTTPS** باشد تا احراز هویت Basic Auth به‌درستی کار کند.

---

## 📝 نکات فنی

- **هزینه‌ها** روی دستگاه ذخیره می‌شوند (Room). WooCommerce مفهوم «هزینه» ندارد؛ بنابراین حسابداری سود/زیان با ترکیب داده‌ی فروش سرور و هزینه‌های محلی محاسبه می‌شود.
- گزارش‌های فروش از endpoint استاندارد `reports/sales` و `reports/top_sellers` ووکامرس گرفته می‌شوند.
- احراز هویت هم از هدر Authorization و هم از query param به‌عنوان fallback استفاده می‌کند.

## نقشه‌ی راه (پیشنهادی)
- همگام‌سازی هزینه‌ها با یک افزونه/CPT سمت وردپرس
- نمودارهای تصویری فروش (bar/line chart)
- خروجی PDF/Excel از گزارش‌ها و فاکتورها
- پشتیبان‌گیری ابری از هزینه‌ها
- اعلان (Notification) برای سفارش جدید
